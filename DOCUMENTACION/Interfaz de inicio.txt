Interfaz de inicio

1. crear una carpeta API (dentro de la carpeta BBDD)
2. En la terminal buscar la ruta inicialmente me sale

	1.  C:\Users\JENYFER\Desktop\JenyferQuintero-TRABAJO-RESTAURANTE-JNC> cd BBDD (escribo cd BBDD) y enter

	2.  C:\Users\JENYFER\Desktop\JenyferQuintero-TRABAJO-RESTAURANTE-JNC\BBDD> cd API (escribo cd API) y enter

3.Inicializar proyecto API escribir en la terminal: 
	   npm init -y


4. Crear la dependencia (express MySQL )
	npm i express mysql mysql2
5. Instalar nodemon como dependencia de desarrollo
	npm i nodemon --save-dev

6. crear el archivo js (index.js) dentro de la carpeta API luego escribir un console.log ('hola) para validar lo que ejecutemos

7. Dentro del archivo packege.json escribir en el "scripts":{
"dev":"nodemon app"

8.Ejecutar el scripts en la terminal:
		npm rum dev 

9.Crear el API:
	1. Importar un archivo o una independencia (vamos al app.js borrar el console.log ('hola).

	2. crear las constantes
		const path =require('path')  (para encontrar la ruta de un archivo - directorio)
		const express=require('express')   
		console.log(__dirname)

	3. crear una instancia
		const app=express()

	4. const puerto=3000  (o el 5000) (dos Enter)

	5.Ejecutar los middlewares (para que la información que llegue al front sea más fácil de manipular)
	app.use(express.json())
	app.use(express.urlencoded({extended:true}))	(dos enter)

	app.use('/',express.static(path.join(__dirname,'../RESTAURANTE')))

	app.listen(puerto,()=>{
   	 console.log('Servidor Iniciado')
	})

	6.	app.post('/inicioSesion',(req,res)=>{
   		 const {usuario}=req.body 
   		 const password=req.body.password
   		 console.log(password);
 		res.json({'usuario':usuario,'respuesta':'inicio de sesion exitosa'})

		})
	
	7. app.use(express.static('public'));
